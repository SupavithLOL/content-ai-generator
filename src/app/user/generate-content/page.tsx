import React from "react";
import GenerateButton from "../_components/GenerateButton";

const generateContentPage = () => {
  return (
    <div>
      <GenerateButton amount={100} type={"Generate"} />
    </div>
  );
};

export default generateContentPage;
